<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Preview Deploy</title>

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

    <!-- Latest compiled and minified JavaScript -->
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
	<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
	<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
	<![endif]-->
  </head>
  <body>
    <div class="container">
      <form class="form-horizontal" role="form">
	<div class="form-group">
	  <label for="envDropdown" class="col-sm-2 control-label">Environment</label>
	  <div class="col-sm-10">
	    <select class="form-control" id="envDropdown">
	      <option value="" disabled selected>Environment</option>
	      {{.envDropdowns}}
	  </select>
	  </div>
	</div>

	<div class="form-group">
	  <label for="nodeDropdown" class="col-sm-2 control-label">Node</label>
	  <div class="col-sm-10">
	    <select class="form-control" id="nodeDropdown">
	      <option value="" disabled selected>Node</option>
	      {{.hostDropdowns}}
	    </select>
	  </div>
	</div>

	<div class="form-group">
	  <label for="jsonInput" class="col-sm-2 control-label">JSON</label>
	  <div class="col-sm-10">
	    <textarea class="form-control" id="jsonInput" rows="3"></textarea>
	  </div>
	</div>
      </form>
    </div>
  </body>
</html>
